# ステージ解法仕様（移行済み）

このファイルは互換リンクのためだけに残している。現行の解法は、各 `src/busybox/stages/S-xxx.tsx` のdefault component直前にある日本語JSDocを正本とする。

各JSDocには、目的、最初の一手、箱ごとの厳密な解法、開かない操作、使用API、権限・privacy、cleanup、対応環境、人手確認を記載している。ここへ解法を複製しない。
