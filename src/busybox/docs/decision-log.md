# 決定ログ

この文書は、確定事項、仮置き、未決事項を混同しないために使う。
後続の実装エージェントは、未決事項を既成事実としてコードへ固定しない。

## 確定している方針

| ID | 決定 | 理由 |
| --- | --- | --- |
| D-001 | タイトルは `Busybox: Web API Explorer` とする | 技術的な核であるWeb APIを名称に残すため |
| D-002 | キャッチコピーは「いつものブラウザが、パズルになる。」とする | 一般ユーザーへ体験を短く伝えるため |
| D-003 | 日英対応を初期スコープに含める | 非言語中心でも権限・設定・プライバシー説明が必要なため |
| D-004 | 静的WebアプリとしてGitHub Pagesで配信する | ブラウザだけで成立する体験と、自前サーバーを持たない方針を両立するため |
| D-005 | 進捗はIndexedDBを主保存先とするローカルファースト構成にする | 未連携でも遊べ、通信やOAuth障害で進捗を失わないため |
| D-006 | Google Drive `appDataFolder` は任意のバックアップ・引き継ぎに使う | ユーザー自身の保存領域を使い、自前DBを避けるため |
| D-007 | 全クリ前提にしない | API、権限、ハードウェア、OS、ブラウザの差をゲームの一部にするため |
| D-008 | Deprecated APIは新規ステージに採用しない | 将来性と保守性を損なうため |
| D-009 | Experimental APIと環境固有APIは採用候補に含める | Webの現在地と環境差を探索する企画だから |
| D-010 | ゲーム上の大区分と内部管理タグを分ける | プレイヤー体験と実装都合を混同しないため |
| D-011 | API名と解法をパズル中に直接説明しすぎない | 発見を体験の中心に置くため |
| D-012 | カメラ、マイク、位置情報などの生データは原則外部送信しない | パズルに不要な収集とプライバシーリスクを避けるため |
| D-013 | 今回の作業は専用ブランチとworktreeで隔離する | 並行作業の未完成変更を混ぜないため |
| D-014 | ステージと問題を箱で表し、ステージは1個以上の問題箱を持つ | 一覧、部分進捗、個別問題を同じ視覚言語で扱うため |
| D-015 | ステージ箱は未着手をリボン付き、一部解決を閉じた箱、完全解決を開いた箱で表す | 説明文に頼らず進捗を伝えるため |
| D-016 | 添付資料は原文スナップショットを保存し、現行仕様と決定ログを正とする | アイデア、古い前提、確定仕様を混同しないため |
| D-017 | ステージを観測、判定、演出、永続化、再挑戦の5層へ分ける | Web API差し替え、テスト、cleanup、進捗保存を分離するため |
| D-018 | `codex/busybox-web-api-game` を唯一のBusybox作業ブランチとする | 過去の試作や計画ブランチとの混同をなくすため |
| D-019 | Googleアカウントを識別・分離せず、選択されたアカウントと単一ローカル進捗をgrow-only統合する | identity scopeと個人情報を増やさず、アカウントをまたぐクリア情報の混在を許容して進捗消失を優先的に防ぐため（2026-07-17決定） |
| D-020 | 全問題箱は色と直下のヒント以外を同じ形状・寸法とし、入場ごとに閉箱から再挑戦する | 問題、履歴、今回の達成を同じ視覚言語で区別し、過去のチェック表示で今回の成否を隠さないため（2026-07-17決定） |
| D-021 | 個別ステージはIDだけを使った `S-xxx.tsx` へ分割し、表示ラベルを識別子に使わない | 2026-08-25に`S-xxx/`フォルダへ完全移行。表示ラベルを識別子に使わない原則だけを継承する（2026-07-18決定） |
| D-022 | 静的な `StageSpec` / `ProblemSpec` と、入場単位の `ProblemHandle` を分離する | 2026-08-25に`StageManifest` / `StageModule` / `StageBoxHandle`へ完全移行。永続クリアと今回の再挑戦状態を混同しない原則だけを継承する（2026-07-18決定） |
| D-023 | Blackbox由来の参考機構は専用台帳で50/50件を追跡し、表現や解法を移植せずWeb固有の独自問題へ再設計してから採否を決める | 調査、未決案、確定仕様、現行コードを混同せず、漏れと不用意な模倣を防ぐため（2026-07-18決定） |
| D-024 | 他アプリの順序・終了を使うID 65相当は、S-250をRGB三色タブと白い監視タブによるWeb Lock解放順の問題へ再設計する | 観測不能なOS状態を捨て、同一オリジンの並行文脈、加法混色、lock lifecycleを組み合わせた独自のWeb問題にするため（2026-07-18決定） |
| D-025 | 非対応、権限拒否、必要機器なしの問題にskipや代替クリアを設けず、未観測のまま残す | 全クリを前提にせず、環境差そのものを観測対象にする既存方針を、問題ごとの成功条件でも維持するため（2026-07-18決定） |
| D-026 | OS音量系の参考機構は、native media playerのシーク、ミュート、再生・停止を独立して観測するS-350の3箱へ再設計する | 取得不能なsystem状態を使わず、user agentのmedia controlsとHTMLMediaElement eventを盤面にするため（2026-07-18決定） |
| D-027 | 端末再起動を要求する参考機構は取りやめ、S-220へdocument navigationのback-forward復帰とreloadを観測する2箱を追加する | 再起動を推測せず、`PerformanceNavigationTiming.type`で直接観測できるbrowser固有の操作を問題にするため。bfcache復元は同じ履歴操作として`pageshow.persisted`でも観測する（2026-07-18決定） |
| D-028 | OS通話の参考機構は、生成音声を送る2タブ間WebRTCの接続成立と明示切断を観測するS-360の2箱へ再設計し、実装前に技術スパイクを行う | 電話状態やmicrophoneへ依存せず、WebRTC lifecycle自体を問題にするため。接続・音声再生・data channel終了の実機成立性が未確認なので、PoC不成立時は成功条件を緩めず再相談する（2026-07-18決定） |
| D-029 | 共有シートの参考機構はS-240をWeb Share送出とWeb Share Target受信の2箱へ再設計し、Busybox自身のPWAインストール導線を共通UIとstage内に用意する | 選択された共有先を推測せず、Webが送信元にも受信先にもなる2方向を使うため。インストールは前提準備として案内するが、それ自体をクリアにはしない（2026-07-19決定） |
| D-030 | OS画面録画の参考機構は、S-190をuser-selected display streamのlive preview、local recording、別tabへのlive relayという3箱へ拡張する | OS録画を推測せず、同じscreen capture sourceをpreview、MediaRecorder、WebRTCという異なる実media pipelineへ流すため。録画Blobとcapture frameは保存・送信しない（2026-07-19決定） |
| D-031 | batteryの参考機構は、hosting deviceについてcharger接続、取り外し、75%以上、75%未満を独立観測するS-370の4箱へ再設計する | charger操作とcapacity帯を分け、別訪問で累積できるようにするため。接続・取り外しは実eventを要求し、capacityは物理証明ではなくbrowser報告値として扱う（2026-07-19決定） |
| D-032 | OS画面輝度の最小・最大を使う参考機構は取りやめ、page内brightness sliderにも置き換えない | 標準の輝度読取APIがなく、deprecatedな非標準APIは不採用であり、CSSだけの置換は既存camera輝度問題より発見が弱いため（2026-07-19決定） |
| D-033 | 生体認証失敗の参考機構は、ゲーム専用passkeyによる正常assertionと、存在しないcredential IDを指定したceremony拒否を観測するS-380の2箱へ再設計する | WebAuthnの失敗理由は区別できないため生体照合失敗とは呼ばず、成功側はpromise resolveだけでなくフロントエンドで署名まで検証する。これはclient内で完結するパズルであり、security boundaryとしての認証ではない。credential残留を事前説明しbest-effort cleanupを用意する（2026-07-19決定） |
| D-034 | D-033のpasskey方式は実装確定を解除し、G-036 / S-380をmemory-only Web Crypto案と再比較する | passkeyはproviderへ残る可能性と削除不能の負担があり、non-discoverable WebAuthnでも残留なしを保証できないため。最終方式の合意までS-380を実装しない（2026-07-19再検討開始） |
| D-035 | memory-only Web Cryptoの署名成功／改変失敗案はG-036 / S-380へ採用しない | browserやOSのauthenticator mediationが発生せず、通常の画面内パズルおよび既存S-020に近く、WebAuthn由来機構を置き換える固有性が不足するため。credentialを作らないWebAuthn rejection / abort案を次の候補として相談する（2026-07-19決定） |
| D-036 | WebAuthn問題にはConditional UIを必ず使い、disposable passkeyとrequest中断をG-036 / S-380へ統合する。G-037 / S-390は独立実装しない | Conditional UIは`autocomplete="username webauthn"`へbrowser固有のpasskey候補を差し込み、discoverable credentialと長時間pendingするrequestを必要とする。credentialなしstageとpasskey stageへ分けるより、一つのLabs stageで成功とplayer起因のabortを扱う方が固有性とlifecycleを保てるため（2026-07-19決定） |
| D-037 | S-380は画面遷移せず、passkey iconとlock iconを中心に、passkey保存、Conditional UI利用成功、保存済みpasskey利用不成立、no-match拒否、pending request中断の5箱を同一pageへ置く | passkey作成・利用と、先に検討したcredential-less request lifecycleを別stageへ分けず、一つのWebAuthn操作盤として比較できるようにするため。lock面は見た目をicon buttonとしつつ、Conditional UIに必要な`autocomplete="username webauthn"`付きinputへ関連付ける（2026-07-19決定） |
| D-038 | D-037の5問題はすべて採用したまま、passkey3箱とcredential-less request2箱のstage境界を技術スパイクで再検討する | 5箱を同一pageに置く操作感と、G-037を仮S-390へ分ける独立性は実際に触らないと判断できないため。問題の採否は再検討せず、5箱統合と3＋2分割だけを比較する（2026-07-19決定） |
| D-039 | system clockを戻す参考機構は、monotonic基準で1時間遅れて進むアナログ時計へOS wall clockを±5分で合わせ、続けてbaseline±5分へ復元するS-400の2箱として採用する | `Date.now()`と`performance.now()`のdriftを直接観測し、画面内の模擬時計ではなくOS設定を入力にするため。第2箱で正しい時刻へのcleanupも問題として促す（2026-07-19決定） |
| D-040 | OS設定の専用toggleを通知権限offへ置換するBB-038案は取りやめ、通知からpageへ戻る機構は既存G-029 / S-090だけを維持する | originの通知権限を`denied`にするとplayerがsettingsで解除するまで他の通知stageが成立せず、OS側でbrowser通知だけをoffにした操作はpageから確実に観測できないため。S-090はService Workerのnotification click専用URLからの復帰をすでに実装しており、追加問題は不要（2026-07-19決定） |
| D-041 | 通知actionの参考機構は、pageへ遷移せず2 actionの入力列をService Worker内で反復処理し、後の通常訪問で達成inboxをconsumeするS-410の1箱として採用する | action clickは`notificationclick`の`event.action`で識別でき、handlerがnavigationを起こさず次のnotificationを表示すれば通知面だけで再挑戦できるため。誤入力は先頭へ戻し、通知権限を変更せず、S-090のnotification本体からの復帰とも分担できる（2026-07-19決定） |
| D-042 | 通知の左右actionを入力列として蓄積し、notification本文からpageへ戻った時に正解列とまとめて比較して金庫の成否animationを行うS-420の1箱を、S-410とは別stageで採用する | S-410の通知内逐次判定とは異なり、通知actionを金庫の組合せ入力、本文clickを提出として役割分担できるため。Service Workerが提出snapshotをcommitしてからround URLを開き、直接URLやquery改変では開かない（2026-07-19決定） |
| D-043 | 通知からinline返信するBB-063は取りやめる | 現行Notifications StandardのNotificationActionはaction ID、title、navigate、iconだけでtext inputやreply payloadを持たないため。action反復による文字選択はS-410 / S-420の重複、pageへ遷移する入力は通常formとなり、通知inline reply固有の問題にならない（2026-07-19決定） |
| D-044 | 指定時刻の通知から復帰するBB-064は取りやめ、server-scheduled Push用のbackendを導入しない | client page / Service Workerには任意時刻の標準alarmがなく、Periodic Background Syncは実行時刻を保証せず、Notification Triggersも一般提供されていないため。static hosting境界を維持し、client timerによる近似問題も作らない（2026-07-19決定） |
| D-045 | 約25分backgroundの参考機構は、新規stageを作らず既存S-040へ25分以上連続hiddenのB02として追加し、既存2秒B01もmonotonic計測へ変更する | Page Visibilityという中心動詞が既存G-018と完全に一致するため。background timerを動かさずvisible復帰時の`performance.now()`差を使い、OS時計変更による誤判定を避ける。reload / discardでdocument memoryを失った場合は試行終了とする（2026-07-19決定） |
| D-046 | OS media controlによる停止の参考機構は、native video playerを使わず、生成loop audioへ届いたMedia Sessionのexternal `pause` action handlerを観測するS-430の1箱として採用する | page内audio pause eventではなくplatform / user-agentのmedia action経路を使い、S-350のnative video controlsと分担するため。Control Center、lock screen、media key、headset、system interruptionはsourceを区別できないので「外側の停止」と表現する（2026-07-19決定） |
| D-047 | home screen上のicon位置を使う参考機構は、manifest shortcut専用URLをinstalled PWAのLaunchQueueで受けるS-310-B02へ置換し、PWA起動周辺の他機能も全件再監査する | icon座標はWebへ公開されない一方、installed app iconのlong press / right clickに出るmanifest shortcutは専用URLで観測できるため。通常page内linkは置かず、既存B01の外部URL再起動とはmanifest task入口として分担する（2026-07-19決定） |
| D-048 | PWA起動周辺の追加ギミックとして、`note_taking`をS-310-B03、File HandlingをS-440、Protocol HandlersをS-450、Window Controls OverlayをS-460、Tabbed Application ModeをS-470へ採用する | OS / browserが所有する入口またはwindow surfaceをそれぞれ使い、通常deep link、shortcut、share targetとは異なる体験になるため。Limited / Experimental機能は対応環境だけで観測し、通常URLやpage内模擬UIによる代替clearは作らない（2026-07-19決定） |
| D-049 | D-048のうちChromeOS限定のTabbed Application Mode案を撤回し、G-045 / S-470は実装しない。File Handlingは固有拡張子`.busybox`をOSで開くS-440として維持する | 特定OSだけに限定されたPWA問題を作らない方針へ変更したため。標準のFile HandlingはOSの「開く」でPWAを起動する機能であり、file dropだけで未起動PWAを起動する標準経路はない。起動済みpageへのHTML dropはBB-086のD&D相談まで分離する（2026-07-19決定） |
| D-050 | BB-027の文字サイズ機構は、`text-scale`とpreferred text scaleを使い、小・標準・大・特大の4帯をそれぞれ別箱へ対応させるG-046 / S-480として採用する | OSまたはbrowserのuser preferenceがCSSの`medium` / `rem`へ入る現行標準を使い、page内sliderやzoomではなく外部設定を入力にできるため。初期状態の帯も正しい入力とし、exact倍率は保存しない（2026-07-19決定） |
| D-051 | BB-079のiOS Spotlight検索は取りやめる | Core Spotlightへの端末内index登録と検索result actionはnative framework限定で、Web indexingから通常HTTPS URLへ遷移してもSpotlight由来を証明する標準payloadがないため。site内検索やdeep linkへの置換は元の中心動詞を失い、S-310とも重複する（2026-07-19決定） |
| D-052 | BB-080のOSスクリーンショットへのQR埋込みは取りやめる一方、「表示とclipboard内容が異なる」独自問題を既存G-006 / S-180の1箱再設計として採用する | screenshot撮影pipelineは観測不能だが、trusted `copy` eventで選択中のCaesar暗号文を平文へ差し替えることは標準Clipboard Eventsで可能なため。平文を実pasteした後、そのDOM内の`busybox`だけをSelectionで選んだ場合に最終箱を開き、単純copy / pasteだけでは開かない（2026-07-19決定） |
| D-053 | `busybox`という文字列を後続問題の鍵として学ぶG-047 / S-490を、placeholderが`busybox`のtext inputへ同じ小文字列を完全一致で入力する1箱として採用する | S-180で突然固有文字列を要求せず、先に弱い記憶を作るため。typingとpasteは区別せず、IME composition完了後を含む実inputの現在値だけを判定し、値や入力履歴は保存しない（2026-07-19決定） |
| D-054 | ステージ一覧の最終形は固定gridから、系統・機構の近さ・手掛かりの継承を線で示す決定的配置のmind mapへ置き換える | 現行gridは進捗一覧としては機能するが、解き方の近さやS-490→S-180のような学習関係を表せないため。箱はsemanticなDOM nodeとして維持し、SVGの線を背面に描き、force layoutは使わない（2026-07-19決定） |
| D-055 | D-052を改訂し、既存G-006 / S-180は逆順の`xobysub`をclipboardへ書き、playerが外で`busybox`へ直して再copyした後、箱click時の`clipboard.readText()`完全一致で開く1箱にする。Caesar暗号chainは新規G-048 / S-500へ分離し、placeholder入力のG-047 / S-490とも別stageにする | S-180のwrite / clipboard受け渡しという既存機構を残しながら、単純copy箱とpaste箱を一つの往復問題へまとめるため。S-490は鍵語の直接入力、S-500はcopy override・paste・Selectionの暗号chainで中心動詞が異なる。D-054の具体的なclue edgeはS-490→S-500とS-180→S-500へ更新する（2026-07-19決定） |
| D-056 | S-190-B04として専用pageのround画像markerを実capture frameからdecodeする箱を採用し、notification欄の画像marker B05は実機PoC後に採否を決める | page markerはuser-selected display streamのpixelを確実な成功根拠にできる一方、notification `image`はLimitedで、OS / browserが通知を共有映像へ含める保証がないため（2026-07-19決定） |
| D-057 | BB-086のアプリ間sticker D&Dは、installed PWAのsource windowから通常browserのreceiver pageへ、round固有payload入りPNGを実FileとしてdragするG-049 / S-510の1箱へ再設計する | iMessageやnative app状態を観測せず、Drag Data Storeがtop-level context境界を越えて画像Fileを運ぶWeb固有の操作にするため。文字列token、同一page内drop、file input、download / uploadはclear根拠にせず、実装前にdesktop browser横断PoCを行う（2026-07-20決定） |
| D-058 | BB-009の近接機構は、W3C `ProximitySensor`の実readingでfarを観測した後に`near === true`となるG-050 / S-520のLabs 1箱として採用する | 2026年Working Draftに直接sensor interfaceがあり、camera遮蔽へ置換せず物理的な近接を観測できるため。非対応、hardwareなし、permission / policy拒否は未観測のままとする（2026-07-20決定） |
| D-059 | Generic Sensor追加問題として、LinearAccelerationSensorのX/Y/Z往復加速3箱をG-051 / S-530、AmbientLightSensorの暗所・明所2箱をG-052 / S-540、GyroscopeのX/Y/Z一回転3箱をG-054 / S-560として採用する | 各sensorの固有readingを中心条件にでき、姿勢、camera輝度、DeviceOrientationとは入力の物理量を分担できるため。いずれもLabsとして実hardwareだけを観測し、値や端末情報を保存しない（2026-07-20決定） |
| D-060 | 端末を投げ上げる／落とす自由落下条件は採用せず、Magnetometerも新規stageに採用しない | raw `Accelerometer`なら自由落下中に3軸が0付近になるという技術的推測は正しい。ただし、その値をクリア条件にすると端末の投擲・落下を通常解法としてゲームが促すため、安全上不採用とする。`GravitySensor`は別interfaceであり、自由落下時の正しさを保証しない。Magnetometer仕様は既定有効のbrowser engineがなく、一般の金属による変化も安定せず、強い磁石を端末へ近づける誘導も避ける（2026-07-20決定、Accelerometerとの区別を同日追記） |
| D-061 | G-056 / S-570として、RelativeOrientationSensorの開始quaternionからX/Y/Zの3姿勢gateを通り開始姿勢へ戻る1箱を採用する | S-560が角速度と累積回転量を扱うのに対し、S-570はmagnetometerに依存しない現在姿勢のpathと閉路を扱うため。raw quaternion列は保存しない（2026-07-20決定） |
| D-062 | D-060の自由落下問題不採用を改訂し、raw `Accelerometer`の3軸合成値が遊びを持った0付近へ入るG-053 / S-550の1箱を採用する。`GravitySensor`固有問題は作らない | raw `Accelerometer`は自由落下中に3軸が0付近となる仕様であり、「端末にかかる加速度が無重力帯へ入る瞬間」を直接観測できるため。初期PoCは合成値2.0m/s²以下を3 reading以上かつ80ms以上とし、実機noiseとsampling rateで閾値だけ調整する。投擲・落下を指示する文章や演出は置かず、raw readingは保存しない。D-060のMagnetometer不採用部分は維持する（2026-07-20決定） |
| D-063 | BB-031の指定語発話を、`SpeechRecognition`のfinal resultが正規化後に`busybox`となるG-057 / S-580のLabs 1箱として採用する | browser固有の音声認識結果を直接使い、text inputや独自ASRによる代替clearは作らない。`busybox`はS-490で先に記憶させ、mind mapにS-490→S-580のclue edgeを置く。音声とtranscriptはアプリへ保存しないが、認識処理がbrowser実装により外部serviceを使いうることは権限説明前に示す（2026-07-20決定） |
| D-064 | BB-033〜036の距離移動を、開始地点から確実に5m、25m、100m離れるG-058 / S-590の3箱として採用する。smartphone sleep / page discard後も同一tab sessionで再開する | Geolocationのaccuracy円を差し引いた保守的な距離でGPS driftによる偽clearを抑える。復帰には開始座標が必要なため、位置情報非保存原則の限定例外として開始座標・accuracy・開始時刻・round IDだけを`sessionStorage`へ最大24時間保存する。経路と途中座標は保存せず、Drive同期・外部送信せず、100m達成またはresetで即削除する（2026-07-20決定） |
| D-065 | BB-051の「ページ外でコピーした文字」は新規stageにせず見送る。Experimentalな`clipboardchange`もこの機構の独立箱には使わない | 既存S-180が、page外で`xobysub`を`busybox`へ直して再copyし、復帰後にclipboardを読ませる体験をすでに持つため。`clipboardchange`で最後の箱clickを自動化しても、playerが発見する中心操作は変わらない。API自体はMDN全件監査へ残す（2026-07-20決定） |
| D-066 | BB-055〜057の高度しきい値を、browser報告高度の100m未満、100m以上500m未満、500m以上へ対応するG-059 / S-600の3箱として採用する | 各訪問で現在位置の該当帯だけを開き、別の高度帯への訪問で累積できる。境界誤判定を抑えるため`altitude ± altitudeAccuracy`の区間全体が一つの帯へ収まるreadingを連続確認する。座標、高度、精度は保存・同期・送信せず、問題箱の達成だけを保存する（2026-07-20決定） |
| D-067 | D-056のS-190-B04 marker表示先を専用pageからmind map型ステージ一覧の外縁へ変更し、BB-060の画面探索機構を同じ箱へ統合する | stage一覧自体を探索空間にし、別tabのmapをpanしてround固有markerを見つけ、そのtabを共有映像へ収める一連の体験にできるため。marker DOMの表示やmap到達では開かず、S-190が実capture frameからpixel payloadを連続decodeした場合だけ開く（2026-07-20決定） |
| D-068 | ステージ一覧はカード全面を単一のbutton操作領域とし、専用の「箱を見る」buttonを廃止する。表示進捗は説明文ではなく累積値`x/n`へ圧縮し、60stageを入力、ページ往来、メディア、PWA、端末、センサーの6近接clusterへ決定的に配置する | 一覧の操作対象と情報量を減らし、APIの内部カテゴリよりplayerが発見する中心操作の近さを優先するため。箱の開閉表現、semantic list、keyboard順、S-190外縁markerは維持する（2026-07-20決定） |
| D-069 | Deep Research元案DR-002のInk APIは新規stage・問題箱にせず、S-160のポインター軌跡描画へ統合する | Ink APIはplayer固有の操作やclear可能な状態を追加しないが、Pointer Eventsで取得する同じ軌跡をOS compositorで低遅延に補間できるため。対応時は一時trailをInk API、永続軌跡をCanvasで描き、非対応時も同じPointer軌跡と成功条件を維持する（2026-07-20決定） |
| D-070 | Deep Research元案DR-013のUI Eventsは、画面上では見えないがDOMのtab順に存在する問題箱をS-150へ追加し、Tab / Shift+Tabによるfocus到達で表示・解錠する | genericなscroll・focus・double-click列は採らず、browserが管理するfocus navigationをplayerの発見操作にするため。pointer hit test、script focus、autofocusでは開かず、semantic buttonとnative focus indicatorを維持する（2026-07-20決定） |
| D-071 | Deep Research元案DR-019のUser Preferences APIはS-480へ統合し、`prefers-color-scheme`、`prefers-contrast`、`prefers-reduced-motion`、`prefers-reduced-transparency`、`prefers-reduced-data`をB05〜B09の独立5箱にする | 各`PreferenceObject.requestOverride()`の成功と実効media queryを観測し、明暗反転、輪郭変化、動作停止、不透明化、軽量表示という別々の描画変化へ直結できるため。OS設定を変更させず、開箱後・reset・離脱時に`clearOverride()`し、非対応・拒否時の代替clearは作らない（2026-07-20決定） |
| D-072 | Deep Research元案DR-021のDevice Memory APIは却下し、新規stage・問題箱にも性能調整基盤にも使わない | `navigator.deviceMemory`は粗く量子化された読み取り専用の端末属性で、player自身が状態を変えられないため。端末class別の鍵は所持端末だけで結果が決まり、内部の品質・負荷調整だけに使ってもAPI固有のパズルにならない（2026-07-20決定） |
| D-073 | Deep Research元案DR-025は、`<details name>`をS-150-B03、`<dialog>`の×button・外側light dismiss・platform cancelを新規S-610の3箱、`<meter>`をS-020の表示UIへ採用し、`<input>`はS-490との重複として追加しない | playerが変えられるHTML固有状態だけを箱にし、dialogは閉じ方を別々のnative経路として観測できるため。meterは読み取り表示に適するが操作対象ではないので成功条件にせず、dialogの`closedby="any"`非対応時は外側click箱の代替clearを作らない（2026-07-21決定） |
| D-074 | Deep Research元案DR-028のCSS Counter Styles API、説明付きcounter、異文明演出は却下する。一方、派生案として17種の実Unicode数字による3桁＋3桁の式、17箱、一つの共通入力欄だけを置くG-061 / S-620を採用する | playerが文字を選択・コピーして外部で調べ、ASCII十進回答を入力すること自体を中心動詞にできるため。Sora Sompengは文化programと固有文字の普及という背景を踏まえて収録し、宗教的由来だけでは除外しない。主に典礼言語として使われるMedefaidrinと、架空・異文明としての演出は収録しない（2026-07-21決定） |
| D-075 | Deep Research元案DR-029のCSS Custom Highlight APIはS-030へ統合し、非連続Rangeを一つのHighlightへ蓄積するB02と、`highlightsFromPoint()`で重なったhighlight集合を直接触って入力するB03を別箱として追加する | 元案の静的な蛍光表示を読んで鍵文入力するだけではauthor側の装飾が中心になるが、B02はnative Selectionが移ってもDOMを分割せず複数Rangeを保持でき、B03は共通paragraphのevent targetでは分からない座標上の複数HighlightをAPIから識別できるため。span分割、`elementFromPoint()`、手計算DOMRectによる代替clearは作らない（2026-07-21決定） |
| D-076 | Deep Research元案DR-030のCSS Font Loading API問題は却下し、font load完了、cache hit、`loadingdone`を問題箱の成功条件にしない | 元案ではplayerはbrowser・app・network側の読込完了を待って表示後の文字を入力するだけで、API固有の操作がないため。font file再投入案はS-130、表示文字の回答案はS-490 / S-620と重なる。S-620等のasset準備・失敗表示へAPIを使うことはできるが、問題採用や箱数には数えない（2026-07-21決定） |
| D-077 | Deep Research元案DR-032のCSS Properties and Values API問題は却下し、typed custom propertyの利用を任意の実装詳細とする | 元案のdialによる値合わせは通常UIであり、S-170のanimation停止とも重なる。登録propertyによる連続補間をS-170 / S-340の演出へ使うことはできるが、既存実装より必ず簡単ではなく、使わなくても仕様欠落としない方針になったため（2026-07-21決定） |
| D-078 | Deep Research元案DR-033のCSS Typed OM API問題は却下し、typed CSS値の利用を任意の実装最適化とする | playerが行うdrag、dial、端末回転は通常UIであり、API固有の操作にならないため。unit-aware演算やStylePropertyMapがS-100 / S-170 / S-340等の実装を簡単にする場合は使えるが、API利用を仕様・成功条件・箱数へ要求しない（2026-07-21決定） |
| D-079 | Deep Research元案DR-036のGeometry Interfaces問題は却下し、DOMPoint / DOMMatrix等を任意の幾何計算補助とする | レンズ・反射問題でplayerが行うdrag / rotateは通常UIであり、APIはapp内部の行列計算にしか現れないため。pointer座標変換、Canvas / SVG描画、hit testで標準型が実装を簡単にする場合は使えるが、API利用を仕様・成功条件・箱数へ要求しない（2026-07-21決定） |
| D-080 | Deep Research元案DR-047のNavigation APIは既存S-220へ統合し、Backで戻った後に別の枝へ進んで旧forward entryを破棄するB04を追加する | browser履歴を操作する中心動詞は既存3箱と同じだが、A→B→Cからbrowser BackでAへ戻ってDをpushし、保持したB / C両entryの`dispose`と`canGoForward === false`を観測する挙動はNavigation API固有の新しい結果になるため。scriptだけのBack、entry数やURLの一致、History API代替では開かない（2026-07-21決定） |
| D-081 | Deep Research元案DR-050のURL Pattern API問題は却下し、新規stage・問題箱を作らない。DR-048やS-220の内部route判定には任意で利用できる | URLPatternは渡されたURLのcomponent matchとgroup抽出を行うだけで、navigationやaddress bar操作を発生させないため。原案でplayerが行うURL組立てはDR-048と同じであり、URLPatternの利用を成功条件にしてもplayerから見えるAPI固有挙動は増えない（2026-07-21決定） |
| D-082 | Deep Research元案DR-063のWeb Speech APIは、既存S-580-B01の音声認識を維持し、位置ごとのalphabet shiftを音声合成だけで返す独立B02として統合する | 既存認識箱へ合成音声を混ぜず、playerが入力を変えて読み上げ結果から+1、+2…の規則を推測し、`aspuwiq`を`busybox`へ変換させる別問題にするため。変換結果は表示せず、対応utteranceの実startと正常endを成功条件に含める（2026-07-21決定） |
| D-083 | 【D-140〜D-142で更新】Deep Research元案DR-069のMedia Capabilities、実frame解像度、字幕track、条件付き音声trackをS-350へ追加する | frame cadenceはD-140でS-810へ分離し、Media Capabilities profile箱はD-141、実frame解像度reelはD-142で不採用とした。字幕trackと対応環境限定の音声trackだけをS-350へ残し、B04にはnative再生速度を採用する（2026-07-21決定、2026-08-10更新） |
| D-084 | 【D-141で不採用】Deep Research元案DR-091のWeb Workers API問題は却下し、Workerを新規stage・箱・成功条件にしない。あわせて既存S-270-B01をWebGPUの可視盤面へ置き換える | WebGPU再設計案を含むS-270全体はD-141で削除した。この行は当時の検討履歴であり、現行計画には使わない（2026-07-21決定、2026-08-10更新） |
| D-085 | Deep Research元案DR-095のバックグラウンドタスクAPIは、DR-118 Prioritized Task Scheduling APIとの重複に変更し、新規stage・問題箱を作らない | 原文の疑似コード`scheduler.postTask(..., { priority: "background" })`はDR-118の中心APIそのものである。`requestIdleCallback()`を意図した場合も、idle periodはuser agent裁量で無期限に来ないことがあり、task完了を待つだけではplayer固有の操作や可視挙動にならないため。非必須処理の内部scheduler利用は妨げない（2026-07-21決定） |
| D-086 | Deep Research元案DR-101のNetwork Information APIは速度推定案を捨て、新規G-062 / S-630へWi-Fi、cellular、ethernet、Bluetoothの4箱として採用する | `effectiveType`、downlink、RTTはplayerが安定して変更できずfingerprinting面もあるが、`connection.type`の具体的な接続方式はplayerが端末外側で選び直せるため。明示的な観測buttonで現在typeに対応する箱だけを開き、初期表示、change eventだけ、速度測定、UA sniffでは開かない。`none`はS-070、saveDataはS-480-B09と重複し、mixed / other / unknown / wimaxは箱にしない（2026-07-21決定） |
| D-087 | Deep Research元案DR-104のStorage APIは問題箱にせず、設定画面へ通常の保存mode、明示的なpersistent storage要求、丸めたusage / quota概算を表示する機能として統合する | `persist()`の許可はuser agent判断を含み、実際のeviction耐性を安全かつ決定的に実証できないため。特別な成功演出や報酬は設けず、app起動時の自動要求も行わない。usage、quota、permission結果は保存・同期・送信しない（2026-07-21決定） |
| D-088 | D-087を撤回し、Deep Research元案DR-104のStorage APIは箱にも設定画面にも追加しない | persistent storageの状態表示や要求はBusyboxに必要な機能ではなく、問題として成立しない案を設定機能へ残す理由もないため。既存のIndexedDB進捗、file export、Drive backupを維持する（2026-07-21決定） |
| D-089 | Deep Research元案DR-114のEncoding APIを新規G-063 / S-640の12箱として採用する | 単一のTextEncoder一致問題ではなく、16文字コードを2進byte列4問、16進byte列4問、文字化け4問の全slotへ各1回だけ割り当てる。文字化けはUTF-8→windows-1252、KOI8-R→windows-1251、KOI8-U→IBM866、Macintosh→x-mac-cyrillicとし、誤読後が元表示から十分変化する固定fixtureを使う。全体解の一意性とWHATWG準拠TextDecoderの結果を自動検証する（2026-07-21決定） |
| D-090 | Deep Research元案DR-118のPrioritized Task Scheduling APIは却下し、新規stage・箱・共通runtime要件を追加しない | priority文字cardは通常の並べ替え、三機械の処理権raceは通常の勝者選択に見える。待機taskの動的昇格を直接操作させるには人工delay、大量task、CPU負荷、または後付けanimationが必要で、安全かつAPI固有のplayer体験にできない。DR-095は同じ`postTask()`機構との重複のまま追加しない（2026-07-21決定） |
| D-091 | Deep Research元案DR-121のPermissions APIは端末診断への統合をやめ、新規G-064 / S-650の4箱として採用する | playerが位置情報、通知、カメラ、マイクをnative promptまたはbrowser site settingsでONにし、対応PermissionStatusが`granted`になった時だけ各箱を開く。初期grantedと`change`の両方を認め、underlying API request成功だけでは開かない。位置、映像、音声、許可履歴は保存せず、取得streamは即停止する（2026-07-21決定） |
| D-092 | Deep Research元案DR-136のOS／外部command連携は破棄し、現行Invoker Commands APIをDR-041 Popover APIへ追加1箱として統合する。DR-025 Dialog側には追加しない | 一つのPopoverへ複数buttonから宣言的にcommandを送り、対象が受けた`CommandEvent.source`と`command`の正しい列を成功条件にすれば、同一document内の呼び出し元と対象の関係を固有操作にできるため。Dialog commandは採用済みの3種類の閉じ方からplayerに見える中心操作を増やさない。DR-041はstage未予約なので正式計画値65stage・153箱は変えない（2026-07-24決定） |
| D-093 | Deep Research元案DR-023のCompute Pressure APIを新規G-065 / S-660の4箱として採用する | playerが観測を開始した後、CPUの実`PressureRecord.state`で`nominal`、`fair`、`serious`、`critical`の対応箱を訪問間で累積する。ゲーム自身はCPU負荷を生成せず、4値を独自の使用率帯へ変換しない。Compute Pressure Level 1にGPU sourceはないためGPU版は追加せず、標準化後に再検討する（2026-07-24決定） |
| D-094 | Deep Research元案DR-031のCSS Painting API問題は却下し、新規stage・問題箱・成功条件に使わない | seed / 周波数を合わせる中心操作は通常のsliderであり、成功判定は同じcustom property値を見るだけになる。Paint Workletのbitmapや実表示完了はmain threadへ返らず、UAが非表示の画像を投機生成する場合もあるため、paint実行を固有の成功条件にできない。既存箱のprocedural背景へ使う場合は任意の描画実装に留める（2026-07-24決定） |
| D-095 | Deep Research元案DR-034のCSSOM問題は却下し、game UIとDevToolsのどちらからもCSS rule編集を解法にしない | game UIでは通常の選択・並べ替えと区別できず、DevTools編集ではDOM、style、script、進捗object等を任意変更できるため、問題の境界がなくなり本作の芯を損なう。DR-120 Console APIはpage編集を要求しないread-only文字画面として別途検討する（2026-07-24決定） |
| D-096 | Deep Research元案DR-120のConsole APIを、read-only Console表示とpage側入力だけで解く新規G-066 / S-670端末迷路1箱、G-067 / S-680端末診断卓1箱として採用する | Console evaluatorからのJavaScript入力やpage編集を要求せず、迷路はASCII盤面と方向button、診断卓は`console.table()`のreadingとpage上のswitch / dialに分離する。Consoleの色、列幅、group UI、open状態は成功条件にしない。S-680のsignal構成と正解条件は実装前に再吟味する（2026-07-24決定） |
| D-097 | Deep Research元案DR-049のURL Fragment Text Directivesを、同一page内のText Fragment linkを繰り返し辿ってhintを集める新規G-068 / S-690の1箱として採用する | fragment directiveとmatch結果はscriptへ公開されないため各jumpの成否、scroll位置、highlightを直接判定しない。playerが実linkで移動して複数の一節を読み、組み合わせた最終回答をpageへ提出した時だけ開く。謎の一節数、巡回順、hint、完全解、総当たり耐性は実装前に再吟味する（2026-07-24決定） |
| D-098 | Deep Research元案DR-065のAudio Session APIを既存G-041 / S-430へB02として統合し、type選択だけで開く案は採らない | playerの明示操作から生成loop音声を`playback` typeで再生し、実`active → interrupted → active`とmedia elementの再生復帰を観測する。interruption sourceは識別・限定せず、ゲーム自身では生成しない。B01のMedia Session pause actionや通常pauseを代替clearにせず、終了時はtypeを`auto`へ戻す（2026-07-24決定） |
| D-099 | Deep Research元案DR-075のRemote Playback APIを新規G-069 / S-700の2箱として採用し、DR-016 Barcode Detection APIをB02の実装先にする | Remote Playbackは外部画面へ任意の別documentを送れず同じmediaをremote再生するため、self-hosted動画のround別区間を使う。B01は接続中の外部画面に出た文字鍵を手元入力し、B02は外部画面に出たround別QRを手元cameraと実`BarcodeDetector`で読む。`prompt()`完了だけ、通常local再生、PiP、固定QR、手入力やJS decoderでは開けない（2026-07-24決定） |
| D-100 | Deep Research元案DR-076のPresentation APIを既存G-069 / S-700-B03へ統合し、外部画面へreceiver pageを実表示できた時点で開く1箱にする | 元案の正しいmessage列パズルは採らない。playerの明示操作から`PresentationRequest.start()`を呼び、実`PresentationConnection`が`connected`になり、外部receiverから表示準備完了messageが戻った時だけ開く。通常の別window、画面ミラーリング、Remote Playback、local模擬messageでは代替しない（2026-07-24決定） |
| D-101 | Deep Research元案DR-077の変換順パズルを作り直し、新規G-070 / S-710の動画変換4箱と、派生G-071 / S-720の映像復元4箱を採用する | S-710は普通の低bitrate動画変換toolに見せ、暗黒frame、decode失敗、QR frame、自己生成metadataへの再入力を別箱にする。S-720はT1左右交換、T2時間方向の二値乗算、T3奇数左・偶数右の選択を複数laneで連結し、4つの固定QR flagを復元する。S-710のmarker QRとS-720 helperはbundled decoderを使えるが、DR-016 / S-700-B02は実`BarcodeDetector`以外で迂回しない（2026-07-25決定） |
| D-102 | 全stageで、事前生成できるmedia fixtureをsource・生成手順・checksumとともにGit管理し、同期・リアルタイム性が本質でない合言葉を固定回答にする | player入力やlive sensorへ依存しない動画・音声・画像をruntime生成する理由はないため。copy可能な回答はギミックを示す長い`BUSYBOX{...}`、映像・外部画面・音声から転記する回答は最大2語とする。round token / nonceは複数contextのcurrent-session照合などreplay拒否自体が中心機構の場合だけ残す（2026-07-25決定） |
| D-103 | Deep Research元案DR-080のWebTransport APIは却下し、新規stage・問題箱・専用backendを追加しない | datagramの欠落許容とstreamの信頼できるbyte列という固有差はあるが、元案の高速hint同期にはWebTransport対応HTTP/3 / QUIC server、Origin検証、証明書、監視運用が必要になる。GitHub Pages中心・自前backendなし方針を維持し、Service Workerや公開echo serverで模倣・外部依存しない。将来自前backendを正式採用した場合だけ別途再検討する（2026-07-25決定） |
| D-104 | Deep Research元案DR-084を新規G-072 / S-730「XRの箱（仮）」の2箱として採用し、XR世界の作り込みは行わない | B01はAR / VRいずれかの実immersive sessionと最初の非null viewer pose、B02は実XRInputSourceのselect rayがXR空間上の単純な箱へ当たることを成功条件にする。元案の現実marker位置合わせ、歩行、room scan、hit test、複雑な空間謎を捨て、inline session、page click、DOM overlay、模擬poseの代替clearを作らない（2026-07-25決定） |
| D-105 | Deep Research元案DR-087のServer-Sent Eventsは却下し、新規stage・問題箱・既存stageへの統合・SSE backendを追加しない | backend負担以前に、playerからは断続的な文字を待つだけでEventSource固有の面白さを操作として出せない。`Last-Event-ID`付き自動再接続で予言を復元する再設計も受動的で、playerへoffline復帰を要求すると既存S-070へ近づく。client timer、Service Worker、静的stream、公開endpointによる模倣も採らない（2026-07-25決定） |
| D-106 | Deep Research元案DR-090のWebSocket APIは却下し、新規stage・問題箱・既存stageへの統合・常時接続backendを追加しない | 遠隔二人が別switchを同時保持する再設計には面白さがあっても、その中心は二人協力でありWebSocket固有ではない。既存S-360へsignaling用途で足してもplayer操作を増やさず、相手待ち、room運用、荒らし対策、server監視を正当化できない。公開echo、模擬socket、`bufferedAmount`大量送信も採らない（2026-07-25決定） |
| D-107 | Deep Research元案DR-093のPush APIは却下し、新規stage・問題箱・S-090への統合・Push送信用backendを追加しない | 本物のPushEventはapplication server→push service→Service Workerで配送される。Dedicated / Shared Workerはowner消失後、Service Workerはevent不在時の生存を保証されず、timerや直接notificationでpage終了後のpushを代替できない。似たbrowser裁量wakeは未相談DR-097 Periodic Background Syncとして残し、今回の代替・統合として消費しない（2026-07-25決定） |
| D-108 | Deep Research元案DR-096のBackground Syncは継続保留とし、新規stage・箱・S-070統合をまだ予約しない | page / tab不在中にも登録済みの仕事を実Service Workerが完了できる特性はAPI固有であり候補として残す。一方、静的JSON取得、IndexedDB受取証、通知だけの現在案ではplayer体験が弱く、通知問題にも寄る。処理対象、離脱中の変化、再訪時の発見が一体になったギミックを得た時に再相談し、時刻指定や30分後alarmには読み替えない（2026-07-26決定） |
| D-109 | Deep Research元案DR-097のPeriodic Background Syncを、新規G-073 / S-740「留守番温室（仮）」の長期Labs 1箱として採用する | 正確な日次成長は捨て、playerが水と光の世話を訪問ごとに一回ずつ預け、各世話の後にwindow client 0件の実`periodicsync`を経た時だけ発芽・開花させる。不定なschedulerを植物の生育速度として受け入れ、期限・countdown・枯死・通知・timer fallbackを設けない。二回の実background eventを要するため必須攻略から外し、固定flagとGit管理済み成長assetを使う（2026-07-26決定） |
| D-110 | Deep Research元案DR-098のBackground Fetch APIは却下し、新規stage・問題箱・既存stageへの統合・低速配信endpointを追加しない | 壊れた画像を表示したまま全windowを閉じ、実download progressの完了後にflag画像を復元する再設計はBackground Fetch固有性を見せられるが、静的GitHub Pages、ブラウザ内Worker、動画の長さでは転送を数時間へ制御できない。stateless Edge Workerが固定画像を`Content-Length` / `Range`付きで低速streamする案は将来のbackend許容時の再相談条件として記録するが、Edge Workerも配信側backendなので現方針には導入しない（2026-07-26決定） |
| D-111 | Deep Research元案DR-102のContent Index APIは現状不採用とし、新規stage・問題箱・既存stageへの統合を追加しない | Chrome Androidのbrowser内蔵content棚からcache済みoffline鍵を開き、browser UIで削除した時だけ届く実`contentdelete`を成功条件にする「持ち歩く鍵」案は次期案として記録する。しかしdesktop Chromeを含むPC browserでAPIと外部一覧を利用できず、user agentによる表示も任意なので、Content Index用のstage IDは予約しない。PC browserで登録・表示・activation・削除eventを実機確認できる実装が出た場合だけ再相談する（2026-07-26決定） |
| D-112 | Deep Research元案DR-105のStorage Access APIは却下し、検討中に派生したcross-origin iframe画像D&Dだけを既存G-049 / S-510-B02へ統合する | `requestStorageAccess()`の許可前にfirst-party cookieを読めない状態をplayerのclear条件へ含めると、第三者cookie policy差と「できないこと」の証明へゲームが依存する。代わりに、外部静的originのiframe内にあるGit管理済み画像layerを親Documentの現像台へ実drag / dropし、browserのDrag Data Storeがorigin / document境界を越えた肯定的eventだけを判定する。既存B01のPWA window→通常browserへのPNG File転送は残し、B02は`text/uri-list`とcurrent iframe payloadを分担する（2026-07-26決定） |
| D-113 | Deep Research元案DR-107のCookie Store APIは体験の重複で却下し、新規stage・問題箱・既存stageへの統合を追加しない | `cookieStore.set()`による封蝋発行は既存storageと、Windowの`change`はcross-context通信と区別できない。Service Workerの購読済み`cookiechange`ならpage不在時のcookie削除を受けられるが、期限切れevictionの時刻を制御できず、player体験もS-740の「状態を預け、閉じ、background event後に戻る」と重なる。APIは将来の非同期cookie実装基盤としてだけ利用可能とし、採用実績や箱数には数えない（2026-07-26決定） |
| D-114 | Deep Research元案DR-115のReporting APIはstageとして却下し、新規stage・問題箱・既存stageへの統合を追加しない | ReportingObserverはCSP違反等の構造化reportをscriptへ渡すが、player向けのbrowser標準UIを持たない。Report-Only CSPで操作を成功させながら実違反reportを得る新規案も、ランプや報告書一覧をゲーム側で自作する体験が中心になる。対応環境での開発用内部診断には任意利用できるが、gameplay、成功条件、外部report endpoint、採用実績には含めない（2026-07-26決定） |
| D-115 | Deep Research元案DR-126のWebOTP APIを、新規G-074 / S-750「届いた封書（仮）」の任意Labs 1箱として採用する | 一箱のclear条件を「current OTPがbrowser所有のOTP専用入力経路から入ること」とする。実`OTPCredential.code`一致、または空の`autocomplete="one-time-code"`欄が一度にcurrent codeへ変わり、trusted input後も実`:autofill`状態で、事前の手入力・paste・drop・compositionがない強いAutoFill判定のどちらかで開く。単なるkeydown不在や`insertReplacementText`だけでは開かない。game serverと電話番号入力を持たず、別の携帯電話または協力者がorigin-bound文面を送る（2026-07-26決定） |
| D-116 | Deep Research元案DR-012のForce Touch eventsは却下し、新規stage・問題箱・既存stageへの統合を追加しない | Apple固有の非標準vendor APIを本作の新規stageへ入れない。対応hardwareの二段押しを使う新規1箱も採らず、標準`PointerEvent.pressure`へ置換するとForce Touch固有性を失ってDR-010と重複するためfallbackや統合にも使わない。stage ID、PoC、assetを予約せず、計画値は75stage・177箱を維持する（2026-07-26決定） |
| D-117 | Deep Research元案DR-017のContact Picker APIを、新規G-075 / S-760「架空の名刺（仮）」の任意Labs 2箱として採用する | 実連絡先から持ち主を探す元案は使わず、固定のname / email / tel / address / iconを持つ架空contactをplayerがOSへ追加する。B01は実pickerから返った5項目の正規化一致、B02はB01後に1件を選択しつつ全5項目が空または欠損で返ることをclear条件にする。B02は同じcontactや共有OFF操作そのものを証明せず、「1件選択したが何も渡さなかった」という観測結果だけを判定する。全property非共有UIは実機PoC gateとし、game製UIで代替しない（2026-07-29決定） |
| D-118 | Deep Research元案DR-052のWeb Componentsはstageとして却下し、新規stage・問題箱・既存stageへの統合を追加しない | 各箱を`<busy-box>`として実装する元案はplayerの解法ではなく実装architectureで、custom element upgradeもgame側のmodule読込、slot配置も既存Drag and Dropとしかplayerには見えない。React `GiftBox` / stage runtimeを全面移行せず、局所的なCustom Elements / Shadow DOM / slot / ElementInternals利用は可能だが採用実績や箱数へ数えない（2026-07-29決定） |
| D-119 | Deep Research元案DR-074のEncrypted Media Extensions APIはstageとして却下し、新規stage・問題箱・既存stageへの統合を追加しない | `org.w3.clearkey`なら暗号化mediaとlicenseをGit管理してbackendなしで成立するため、実DRM・license server必須という暫定理由は撤回する。一方、仕様がplayerへ保証するのは鍵不足時の再生suspendと`waitingforkey` eventで、鍵icon、error文、spinner等のEME専用default UIはない。genericな動画停止・再開を説明するgame製UIが中心になるため採らない（2026-07-29決定） |
| D-120 | Deep Research元案DR-083のWebVR APIはstageとして却下し、新規stage・問題箱・既存stageへの統合を追加しない | `VRDisplay.requestPresent()`で箱の裏を覗く元案はDeprecatedな旧APIに依存する。現代の代替であるWebXRの実immersive sessionと空間上の箱へのinteractionは既存G-072 / S-730の2箱へ採用済みであり、裏へ回る探索を足すとXR世界を作り込まない既決方針からも外れる。WebVR互換展示やWebXRへの重複加算は行わない（2026-07-29決定） |
| D-121 | Deep Research元案DR-086のXMLHttpRequest APIはstageとして却下し、新規stage・問題箱・DR-085 Fetch案への統合を追加しない | XHR自体は現役Living Standardであり、readyState 0〜4やdownload / uploadのProgressEventは固有である。しかし古い通信儀式や5灯通信盤としてplayerへ見せる部分はgame製UIで、XHR自身にbrowser所有の表示はない。response header / body、download進捗、中断はFetch案でも近い体験になり、同期XHRのDevTools警告も非保証かつ削除方向なので使わない（2026-07-29決定） |
| D-122 | Deep Research元案DR-100のBeacon APIを既存S-060-B02「オフライン郵便（仮）」へ統合し、1箱を追加する | B01の単純再訪は残す。onlineでService Worker制御とcacheを準備した後、playerがoffline中に明示投函し、実`sendBeacon()`がqueueを受理した時だけreceiverへのfull-document navigationを続ける。Service Workerが専用same-origin POSTを検証し、`respondWith()`中にIndexedDB receiptをcommitした事実で開く。`true`だけ、tab close、`visibilitychange`、same-document遷移、通常fetch、foreground write、単純再訪では開かない。実Chromium PoCでbackend停止中のBeacon→Service Worker→IndexedDB受領を確認した（2026-07-29決定） |
| D-123 | Deep Research元案DR-108のShared Storage APIはstageとして却下し、新規stage・問題箱・既存stageへの統合を追加しない | 元案の「世界全体の進捗」は各browser profile内のcross-site local storageから得られない。二つのtop-level siteをまたぐ第三者originの記憶をworkletと`selectURL()`でFenced Frameへ出す新規案も、playerが見るのはgame製contentでbrowser所有UIがなく、別site・第三者origin・privacy sandbox enrollmentに見合う固有操作にならない。WICG repositoryはarchive済みで、Chrome M144でdeprecated、M150以降の無効化とM152の実装削除が進行中のためhistorical exhibitも作らない（2026-07-29決定） |
| D-124 | Deep Research元案DR-117のJS Self-Profiling APIはstageとして却下し、新規stage・問題箱・S-680 Console診断卓への統合を追加しない | 元案は実装者がcodeを最適化してself timeを減らすものでplayer操作ではない。実traceからhot functionを答える診断卓も、player向け表示はgame製formatter / Console APIでありSelf-Profiling固有UIがない。samplingは端末、JIT、timingで揺れ、安定化に不要なCPU負荷を要し、固定traceではAPI固有性を失う。内部のlocal開発診断への任意利用は妨げないが採用実績へ数えず、player traceを収集・送信しない（2026-07-29決定） |
| D-125 | Deep Research元案DR-119の投機ルール APIはstageとして却下し、新規stage・問題箱・S-220への統合を追加しない | 元案の滑らかさはcacheや高速回線と区別できない。targetが`document.prerendering`とactivationを観測するS-220-B05「先に入っていた部屋」案ならgame側は実prerenderを証明できるが、playerが見るのは到着時の開箱や経過表示というgame製演出だけで、browserは投機中の専用UIを出さない。DevToolsを解法にせず、内部navigation高速化へ任意利用しても採用実績へ数えない（2026-07-29決定） |
| D-126 | Deep Research元案DR-127のFederated Credential Management APIを、新規G-076 / S-770「身分証棚（仮）」として採用する | 実装着手時に公式情報を再調査し、公式FedCM提供、一般向けRP登録、provider自身または信頼できるmanaged IdP、FedCM経路の肯定的証明、Busybox独自backend不要を満たすserviceごとに任意箱を置く。Google 1箱を現計画の下限とし、追加providerはclient登録と実account PoC後に箱数へ加える。JWTやaccount属性をdecode・照合・保存せず、OAuth redirectやbroker越しの通常loginをFedCM箱へ数えない（2026-07-30決定） |
| D-127 | Deep Research元案DR-128のPayment Request APIはstageとして却下し、新規stage・問題箱・既存stageへの統合を追加しない | Payment Requestはmerchant、payer、実payment methodを仲介するAPIで、模擬決済modeではない。Deprecatedな`basic-card`に依存せず、実Google Pay / Apple Pay、総額0、cancelだけの箱も使わない。payment credentialやpayer情報を要求せず、架空payment appをService Workerで扱う可能性だけをDR-129 Payment Handler APIへ分離して相談する（2026-07-30決定） |
| D-128 | 【D-147で4箱へ改訂】Deep Research元案DR-129のPayment Handler APIを、新規G-077 / S-780「三つの財布（仮）」3箱として採用する | Git管理する架空payment methodと架空handlerだけを使い、trusted `PaymentRequestEvent`を証跡として観測する。承認後の`complete("success")`、意図的拒否後の`complete("fail")`、同一handler内の`PaymentResponse.retry()`後の成功を別箱にする。handler選択・merchant側の開始経路は固定せず、返ったresponseに対応する箱だけを開く。各条件成立時に箱だけを開き、取引結果label、完了message、固定flagは表示しない。実payment method、payer情報、game製payment sheet、cancel / errorの代替clearを使わない（2026-07-31決定） |
| D-129 | 全般のパズル本体、手掛かり、開箱結果を可能な限り非言語にし、API操作だけで直接開く箱へ使途のない完了message・状態label・固定flagを追加しない | 箱がその場で開く反応自体が成功表示になるため。固定回答は文字列の発見・変換・転記が中心機構である問題に限定し、権限・privacy・error・accessibilityの必要説明は非言語性を理由に削らない（2026-07-31決定） |
| D-130 | Deep Research元案DR-130のAttribution Reporting APIはstageとして却下し、新規stage・問題箱・既存stageへの統合・historical exhibitを追加しない | GoogleがAPIのretireを発表し、Chrome 144からdeprecationと削除を進めている。source / trigger照合はbrowserの非公開領域で行われ、固有UIやclient側の確定eventがなく、遅延・noise付きreportを受けるbackendが必要である。URL / referrer等への置換はAPI固有性がなく既存体験と重複する（2026-07-31決定） |
| D-131 | Deep Research元案DR-131のPrivate State Token APIはstageとして却下し、新規stage・問題箱・既存stageへの統合・demo issuer依存・historical exhibitを追加しない | API自体は継続サポート対象だが、本番のtoken発行・償還には登録済みissuer、独自issuer / redeemer backend、暗号鍵の安全な運用が必要で、Busyboxの架空儀式を任意条件にできる一般向けmanaged issuerはない。通常playerへ発行・保存・償還のbrowser固有UIもなく、DevToolsを解法にしない方針ではgame製反応しか見えない（2026-07-31決定） |
| D-132 | Deep Research元案DR-132のTopics APIはstageとして却下し、新規stage・問題箱・既存stageへの統合・historical exhibit・browser設定変更を要求する箱を追加しない | Chrome 144からdeprecationと削除が進み、非標準で他browser vendorも否定的である。複数siteと週単位epochから推定される関心topicはplayerがその場で制御できず、noiseと履歴不足で非決定的である。通常player向け固有UIもなく、閲覧傾向の推定結果をgameplayへ使わない（2026-07-31決定） |
| D-133 | Deep Research元案DR-134のFenced Frame APIはstageとして却下し、新規stage・問題箱・既存stageへの統合・historical exhibitを追加しない | 隔離枠内の達成を親pageへ直接通知できず、内部だけで箱を開いても通常の進捗保存・同期へ接続できない。`reportEvent()`は親への同期eventでなくreporting先へのbeaconであり、backendによる迂回を採らない。playerには普通の埋め込みcontentにしか見えず、frame内の任意puzzleや通常navigationへの置換もAPI固有体験にならない（2026-07-31決定） |
| D-134 | Deep Research元案DR-137のLocal Font Access APIを、新規G-078 / S-790「活字の鍵（仮）」1箱として再設計して採用する | 端末固有のinstalled font集合から頭文字を作る元案は破棄する。Git管理する専用OpenType fontをplayerがOS標準UIでinstallし、明示走査とbrowser所有permissionを経て対象PostScript名だけを`queryLocalFonts()`へ要求する。実`FontData.blob()`の期待dataを検証して専用glyphを表示した時だけ直接開き、全font列挙、既存font、file upload、`local()`だけ、固定flagを使わない（2026-08-01決定） |
| D-135 | G-067 / S-680 Console診断卓を不採用とし、予約した1stage・1箱を実装計画から外す | Consoleをread-onlyの別画面、pageを入力面として往復する体験はG-066 / S-670 Console迷路で既に成立する。switch / dialと`console.table()`へ置換しても中心体験が重なるため、別の謎を追加しない。Console APIの採用実績はS-670へ集約し、計画値を78stage・185箱とする（2026-08-09決定） |
| D-136 | Text Fragmentを、既存G-068 / S-690の巡回1箱と、新規G-079 / S-800のURL組み立て2箱という二つのstageに分けて採用する | S-690は最初のlinkから同一page内の断片を辿り、集めたhintの最終回答で開く。S-800は同一pageの英文と、fragmentを示すB01、単語を示すB02を置き、入力欄を使わずplayer自身がfragment付きURLを組み立てて対象語だけをUA highlightさせる。`textStart`を空白、suffixを`.`とする指定は任意の中間単語を含むmatchにはならないため採らず、B01はpercent-encoded `textStart`とpunctuation contextで対象語を直接読ませない。通常の可視Text Fragment一致範囲はscriptから取得できないため、箱の自動開放には`hidden="until-found"`と`beforematch`を使い、対象語が出現してhighlightされる構成を採る。具体的な英文、対象語、fragment、S-690の巡回graphと最終回答は各stage実装前に別途吟味する。計画値は79stage・187箱とする（2026-08-09決定） |
| D-137 | 【D-140で現在値とS-350構成を更新】現環境でAPIまたは中心経路を確認できた17stageを一括実装し、製品stage上の確認でPARTIALを箱単位に減らす | PoCを完全な直列gateにはせず、S-020、S-030-B02、S-060-B02、S-150-B02/B03、S-220-B04、S-270、S-350-B04〜B07、S-510-B02、S-580-B02、S-610、S-620、S-640、S-650、S-660、S-670、S-710、S-720をWave 0〜6で実装する、という当時の実装判断。68stage・157箱と79stage・187箱はこの時点のスナップショットであり、現行の箱ID・箱数・解法には使わない（2026-08-09決定、2026-08-10更新） |
| D-138 | S-660の`fair`と`serious`を一つの中間状態箱へ統合する | Compute PressureはCPU使用率の等分値ではなく、UA実装定義の離散hintを返す。ゲームが負荷を生成しない前提では中間二値を別々に必須化せず、`nominal` / 中間 / `critical`の3箱にする。未公開のため旧箱構成との進捗互換処理は持たない（2026-08-10決定） |
| D-139 | 初回公開までは過去のローカル実装とのバージョン互換性を要件にしない | schema、stage ID、fixture、保存データを変更する際は現行設計を単純に保ち、未公開版だけを対象とするmigration、fallback、deprecated経路は追加しない。初回公開時に互換性基準となるversionを固定する（2026-08-10決定） |
| D-140 | 【D-141でS-350箱数と全体件数を更新】S-350のframe cadence箱を新規G-080 / S-810へ分離する | VFR timeline内の24fps区間探索はS-350とは独立した発見になる。S-810はplayerによる実seek後の`requestVideoFrameCallback().mediaTime`だけで判定し、script自動seekやwall clockを代替条件にしない（2026-08-10決定、2026-08-10更新） |
| D-141 | S-270を削除し、S-350のMedia Capabilities profile箱を不採用にする | S-270はWebGPU特有のplayer体験を成立させられないため、G-024、catalogue、runtime、map、manifest、実装から外す。Media Capabilitiesはauthorが候補構成の`decodingInfo()`を問い合わせるAPIで、字幕trackのようにplayerがnative controlsから実解像度を選ぶ標準UIではないため箱にしない。S-350の現行箱はB01〜B05へ詰め、承認済みの対応環境限定音声trackは指定どおり将来B07として予約する。現行コードは68stage・154箱、全採用計画は79stage・185箱（2026-08-10決定） |
| D-142 | S-350-B04の実寸reelを削除し、native再生速度変更へ置き換える。PiPはS-230へ集約する | 実寸reelはpage製selectorしかなくbrowser特有のUIを使わない。`playbackRate`と`ratechange`はnative controlsの速度変更を実media状態として観測できるためB04へ採用する。PiPはS-230-B01が`enterpictureinpicture`をすでに成功条件としており、S-350へ重複追加しない。B03のclueをpause icon、B05をsubtitles iconへ変更する。箱数は68stage・154箱、全採用計画は79stage・185箱のまま（2026-08-10決定） |
| D-143 | D-142のPiP配置だけを改訂し、S-230-B01をS-350-B06へ移して独立S-230を削除する | seek、mute、play-pause、速度、字幕と同じ一つのnative videoからPiPへ入る構成の方が、browser所有media controlsを一か所で吟味できる。S-350はpage製PiP buttonや自動`requestPictureInPicture()`を置かず、実`enterpictureinpicture`だけを観測する。現行コードは67stage・154箱、全採用計画は78stage・185箱（2026-08-10決定） |
| D-144 | media stageを製品UXへ再構成し、PoC資産を製品fixtureへ昇格する | S-350へ同じnative videoのfullscreen箱B08を追加する。S-640は分類別cardと12問共通入力にする。S-710は独立ClipPress HTMLをsame-origin iframeへ埋め、downscale QRをbundled jsQRで検出し、decode失敗outputを固定fixtureにする。S-720は3動画source・3変換・1出力をBezier cableで接続するpatch bayと共通flag欄にし、4正規routeで固定復元動画を出す。PoC配下のencoding / Unicode / video recovery assetは製品fixture領域へ移し、製品stageからPoC pathを参照しない。現行コードは67stage・155箱、全採用計画は78stage・186箱（2026-08-10決定） |

| D-145 | 固定fixtureの版管理はGitを正本とし、隣接する独自SHA-256一覧を持たない。第三者ライセンスは配布物へ原文を同梱し、Busybox共通フッターから一覧ページへリンクする | Gitのコミットが固定assetの同一性を管理するため、同じ内容をSHA一覧で二重管理すると更新漏れを招く。codec・寸法・フレーム・意味内容・WOFF2署名・ライセンスなどの意味検証は残す。SHA-256はS-510のpayload、S-790の実FontData、暗号・同期プロトコルなど判定そのものの場合だけ使用する。jsQR、MediaBunny、GNU Unifontのライセンス原文と対応するsource linkは`src/public/busybox/licenses/`へ同梱し、Service Workerのshellにも含める（2026-08-11決定） |
| D-146 | S-810をVFR cadence判定からnative seek停止時のアスペクト比判定へ更新する | 固定MSE assetの提示frameをplayerが自分で探す体験を残しつつ、1:1、4:3、16:9、9:20の4箱へ明確に分ける。`seeked`後の`requestVideoFrameCallback()`で実`videoWidth / videoHeight`を読み、各相対5%以内だけを開く。通常再生、pause、CSS寸法、script自動seekは成功条件にしない（2026-08-16決定） |
| D-147 | S-780を4箱へ拡張し、browser所有chooserで指定walletを選ぶ問題を追加する | ○/◇の2 Payment Appを同じ架空methodへ登録する。B01承認、B02拒否、B03再試行はwalletを限定せず従来どおりにし、B04だけは◇workerへcurrent requestのtrusted `PaymentRequestEvent`が届いた時点で開く。その後のApprove / Decline / retryは限定せず、game製picker、page click、登録済み判定、stale / untrusted messageを代替clearにしない。通常のGitHub Pagesでは任意routeの`Link: rel="payment-method-manifest"` response headerを設定できないため、公開時はheader ruleを持つmanaged static hostを用いる。現行コードは69stage・159箱（2026-08-18決定） |
| D-148 | POC-035〜054の個別レビューを完了し、10件を新規stage候補、10件をstage不採用へ確定する | 採用はPointer Lock 3箱、Idle Detection 2箱、IntersectionObserver 1箱、Document PiP 1箱、EditContext 3箱、File System Access 3箱、Compression Streams 3箱、任意element Fullscreen 1箱、MediaSource継ぎ足し1箱、runtime WebVTT cue生成1箱。不採用はKeyboard Layout Map、URL navigation、Sanitizer、WebGL2、Fetch conditional、MessageChannel、File and Directory Entries、Streams、Trusted Types、WebCodecs。PoCがpositiveでも、player固有の操作やbrowser / OS所有surfaceがなくgame製UIだけで代替できるものはstage化しない。内部実装での利用は採用stage・箱・API件数へ数えない。新規stage IDと全体件数は実装時まで予約・加算しない（2026-08-20決定） |
| D-149 | D-148採用stageの既存stageとの差分と実装前ゲートを固定する | Fullscreenはvideo player stage S-350へ統合せず任意HTML要素内clickの専用stage、MediaSourceは完成済みMSE timelineをseekするS-810と分けてplayer自身がsegmentをappendするstage、WebVTTは固定字幕を選ぶS-350-B05と分けて再生中に`VTTCue`を生成するstageとする。Compression Streamsは3形式の伝達性を製品UXで再評価し、MediaSourceの具体的な映像内容はfixture生成前に再相談する。他の成功条件・負例・cleanupは`next-poc-and-stage-work.md`を現行キューの正とする（2026-08-20決定） |
| D-150 | DR-041 Popover APIを新規G-091 / S-920の3箱クリック迷路として実装する | hoverやcommand列の推理ではなく、宣言的invokerで入れ子Popoverを開閉し、固定treeの3終点にある実箱を押す体験へ確定する。各goalの影は、実経路と同じ部屋寸法・十字button位置・`position-area`・`position-try-fallbacks`を持つ非操作CSS anchor chainの終点として常時layoutする。座標をJavaScriptで測定・転記せず、画面幅・画面端で配置が変わっても同じCSSアルゴリズムで実goal Popoverと影を一致させる。runtimeの経路・座標判定はせず、既知goal内のtrusted clickだけを開箱条件にする（2026-08-21確定） |
| D-151 | S-920の迷路座標系を同一origin iframeの固定viewportへ閉じ込める | iframe外周をPopoverが越えられない実際の表示不可領域にする。外周は斜線の額縁で見せ、B01はinline、B02はblock方向の`position-try-fallbacks`を必ず踏む。実経路と影経路は同じiframe viewport内でbrowserのCSS layoutへ同時に追従し、親page scroll、iframe resizeともJavaScript再測定を行わない（2026-08-21決定） |
## 仮置きしている事項

| ID | 仮置き | 変更してよい条件 |
| --- | --- | --- |
| T-001 | 大区分は5系統とする | API棚卸しまたはMVP試作で分類が体験を損なうと分かった場合 |
| T-002 | 大区分の仮称に「基盤」「端末」「保存」「通路」「実験」を使う | アート・ゲームデザイン検討でより適切な名称が決まった場合 |
| T-003 | 初期版は各系統から少数のステージを選ぶ | 試作評価に必要な組み合わせが変わった場合 |
| T-004 | 既存リポジトリ内の1つのViteエントリとして配置する | PWAスコープやService Worker分離に重大な問題がある場合 |

## 未決事項

| ID | 論点 | 決定に必要な材料 | 決定期限 |
| --- | --- | --- | --- |
| O-001 | 箱を中核にした最終アートディレクションと大区分名 | 競合作品調査、複数の画面試作、非言語での理解度 | UI実装前 |
| O-002 | 初期ステージの正確な組み合わせ | API再調査、試作コスト、対応環境、ギミック重複確認 | ステージ実装前 |
| O-003 | ステージ一覧の最終レイアウト | D-054でmind mapを採用済み。枝数、mobile viewport、keyboard順を技術スパイクで確定する | 解決済み・D-054参照 |
| O-004 | どの途中状態をDrive同期するか | 競合例、データ量、別端末UX | Drive実装前 |
| O-005 | Google OAuth公開設定とブランド確認の範囲 | Google Cloud Consoleの現行要件、公開ドメイン | 公開設定前 |
| O-006 | サーバー依存APIを本編へ含めるか | 静的配信との両立方法、外部依存の保守コスト | API拡張フェーズ |
| O-007 | アクセシビリティ用ヒントの粒度 | 非言語性との両立、テスト参加者の所見 | 初期リリース前 |
| O-008 | `Busybox` の最終名称利用可否 | 商標・検索・既存OSSとの混同調査 | 公開告知前 |

## 更新ルール

- 決定を変える場合は、既存行を書き換えるだけでなく、理由と日付を追記する。
- ブラウザ対応状況のように時間で変わる情報は、この文書ではなくAPI調査台帳へ記録する。
- 実装都合だけでプロダクト方針を変えない。変更が必要なら、先にこのログへ論点を追加する。
