---
description: すべてのプロジェクトの基本となるコーディングルール
globs: 
alwaysApply: true
---

# 定義したコーディングルール

このリポジトリ内すべてのプロジェクトで基本となるコーディングルールを記載する。

## インポート方法

可能な限りdefault importを避け、named importの形をとる。

## 状態管理

状態管理とコンポーネントとは必ずファイルを分ける。コンポーネントファイルから状態管理ライブラリはインポートしない。そのため、必要あれば状態管理ファイルでuse関数をexportする。